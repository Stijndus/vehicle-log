import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vehiclelog.app',
  appName: 'VehicleLog',
  webDir: 'dist/app/browser',
};

export default config;
